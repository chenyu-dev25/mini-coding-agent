# Build Log

## 开发过程记录

这次评测题的开发过程，核心不是“分别用了几个 AI 工具”，而是我怎么组织这些工具去完成一个明确目标。

我这次的整体方式可以概括为：

- 底层主要执行者是 **Codex**
- 中间主要通过 **gas-town** 这个 multi-agent 产品来编排和操纵 Codex
- 最后再用 **Cursor** 做缝缝补补、收尾和整理

所以严格来说，并不是我先独立用 Codex、再独立用 gas-town、最后独立用 Cursor。  
更准确的说法是：

**我主要是通过 gas-town 去调度和驱动 Codex 完成主体开发，再用 Cursor 完成后期修补和交付整理。**

## 我的核心方法：Goal-Driven

如果要总结这次开发里我最重要的思考，我会写成一句话：

**我认为 AI 时代下的编程，应该是 Goal-Driven，也就是以目标为导向。**

对开发者来说，这意味着：

- 先定义要完成的目标
- 先想清楚验收标准
- 再去组织工具、代理和实现路径
- 任何实现都要反过来问一句：它是否真的在服务目标

说大白话就是：让一个master agent不断监督subagent工作，直到subagent完成了工作，并且master agent亲自认证工作效果，符合判据中的要求，否则就继续无限循环，逼迫subagent持续工作。 **这也是为什么我使用gas-town来编排和操纵Codex的原因。**

同时：**一个纯粹目标驱动的框架，根据criteria判据判断达不到目标，必须收集一切错误信息进行改进，进行无限循环，直到彻底实现最终goal（目标）为止。**。当然，代价就是大量的token，**token is all you need**。(如果入职了的话，希望公司可以帮我把token报销了hhhh，我目前在京东基本一个月要花一千自己买token)

这也是我这次为什么会不断做取舍。

我不是单纯追求“多写点代码”或者“多加点模块”，而是一直围绕这道题本身去判断：

- 这个项目是不是已经从单 Agent loop 演化成了 skill-centric system
- Skill 和 Tool 的边界是不是足够清楚
- Router、Trace、Fallback 是不是真的发挥了作用
- 最后的结构是不是足够清晰、可运行、可解释

所以：**确定目标**

## 第一阶段：通过 gas-town 编排 Codex，完成主体系统

这次开发的主体阶段，我主要是通过 **gas-town** 来驱动 **Codex** 完成系统构建。

在这个阶段里，gas-town 更像是一个多代理调度层，而 Codex 是核心执行能力。  
我主要借助这种方式来推进整个系统的主体实现。

这一阶段做的事情包括：

- 阅读和理解原始 `mini-coding-agent` 仓库
- 判断哪些原始结构应该继承，哪些应该重构
- 设计多 Skill Agent 的整体方案
- 增加新的 runtime 层，包括：
  - `app.py`
  - `executor/`
  - `router/`
  - `models/`
  - `trace/`
- 搭出第一版 Skill Router、Trace 和失败处理机制
- 验证整体结构是否能够覆盖 `issue.md` 的主要要求

---

> **`Master prompt｜本阶段验收判据`**
>
> 以下内容即 **gas-town Master** 侧用来验收子代理、判断「主体阶段是否过关」的判据（与上文 Goal-Driven 一致）；**不是**单纯功能堆砌清单。
>
> - **题目硬指标**：显式区分 Skill 与 Tool；至少 4+ 个带 schema 的 skill；具备 router 选链（非全员同一条死流水线）；至少 3 类任务且其中 2 类明显多 skill；有可展示的 Trace；至少有 verify / retry、ask-back、或同类失败兜底之一；扩展上新增 skill 不必大改 executor 主流程。
> - **可运行**：本机 `uv sync` 后能跑通 Skill 入口（`uv run mini-coding-agent`）及关键路径，核心行为有 `pytest` 覆盖或可被人工一条命令复现。
> - **可解释**：阅卷人能从 trace / `DESIGN.md` 看出「为什么选这条链、中间结果如何接到最终答案」，而不是单模型黑盒。
> - **与原始 harness 的关系能说清**：保留 `mini-coding-agent` 演化痕迹（如仍保留 `mini_coding_agent.py` 与 `tools/`），新 runtime 的职责边界在文档里可陈述。

---

满足以上 **Master 判据**，我才认为第一阶段「方向成立」，可以进入后续校准与 Cursor 收尾；否则继续在 gas-town 循环里改架构而不是抛光局部。
(PS: 如果公司愿意给我足够的token， 我会把判据改成几百个unit test)
这个阶段最重要的不是某一个具体文件怎么写，而是：

**把系统先搭起来，并验证这个方向是成立的。**

换句话说，我在这一阶段最看重的是：

- 目标能不能达成
- 框架是不是成立
- 结构是不是对的

而不是一开始就在局部细节上打磨太久。

## 第二阶段：持续校准实现，避免“功能堆叠”

在主体实现推进的过程中，master agent也一直在调整自己的判断。

因为我发现 AI 编程一个很容易出现的问题是：

**系统会越来越会“生成东西”，但不一定越来越接近目标。**

所以在中间阶段，master agent会不断回头检查：

- 现在加的这部分复杂度是不是必要的
- 它是不是只是“看起来做了很多”
- 它有没有真正提高 skill 系统的清晰度
- 它是不是更符合题目的验收要求

也正是因为有这种反复校准，后面我才会不断做减法和收敛。

## 第三阶段：使用 Cursor 做缝缝补补和最终收尾

在整体系统已经基本成型之后，我主要使用 **Cursor** 来做后期处理。

这一阶段 Cursor 的角色，就是纯粹的缝缝补补。

这个阶段主要处理了：

- 和 `main` 的 diff 收敛
- 多余代码的清理
- 文档补充和重写
- 一些 CLI、示例、说明文档的修正
- 最终交付材料的整理

所以如果用比较直白的话说：

- **gas-town + Codex** 负责把主体系统做出来
- **Cursor** 负责把已经成型的系统修顺、补齐、整理到更适合交付的状态

## 为什么我会这样组织这次开发

因为我越来越觉得，AI 时代下写代码，不应该只是“谁能生成代码就让谁写”。

真正重要的是：

- 目标怎么定义
- 路径怎么组织
- 工具怎么协作
- 最终结果怎么验证

这也是为什么我会强调 Goal-Driven。

我的理解是：

1. 目标先于实现
2. 验收标准先于细节设计
3. 工具是为目标服务的，不是反过来主导目标
4. 如果一个实现不能更接近目标，就应该删掉或收敛

这次项目里很多重构和清理动作，本质上都是这个思路的体现。

## 最终结果

最后形成的是一个基于 `mini-coding-agent` 演化出来的多 Skill Agent 原型，具备这些能力：

- 显式区分 Skill 和 Tool
- 使用目录式 markdown 定义 Skill Schema
- 具备 Skill Router / Skill Selection
- 支持多类任务
- 具备 Trace / 可解释性 / 可观测性
- 具备 ask-back 和 verify/retry 等失败处理机制
- 保留了原始 CLI 风格，同时完成了 skill-centric runtime 的增强

## 总结

回头看这次过程，我会把它总结为：

- 我以 **Goal-Driven** 的方式来推进整个开发
- 我主要通过 **gas-town** 这个 multi-agent 产品去编排和操纵 **Codex**
- 再用 **Cursor** 去完成最后的缝补、修整和交付化处理

对我来说，这次过程最大的收获不是“会用了几个 AI 工具”，而是更明确了一点：

**AI 时代的编程，重点不只是生成代码，而是如何围绕目标去组织生成、判断、收敛和交付。goal-driven is everything.**
